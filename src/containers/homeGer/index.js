import React, { Component } from 'react';
import Navbar from '../../components/navbar';

export default class HomeGer extends Component{
    render(){
        return(
            <Navbar/>
        );
    }
}